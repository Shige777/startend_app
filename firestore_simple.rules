rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証済みユーザーのみアクセス可能（開発用）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 